# Базовые зависимости пайплайна
requests>=2.31

# Облачный фолбэк (gemini_fallback.py). Нужен только если планируешь добивать
# проблемные сегменты облачным Gemini. Локальный пайплайн работает без него.
google-genai>=0.3

# Опционально - для более точной нарезки на предложения (stage1 --method kss).
# Без них работает режим regex (по умолчанию).
# kss>=4.5
# pecab
